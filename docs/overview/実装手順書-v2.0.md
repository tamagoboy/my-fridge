# 冷蔵庫管理アプリ 実装手順書 v3.0

> 要件定義書 v4.0（Next.js / PostgreSQL / Prisma）に準拠

## 基本方針

- **安定した土台を先に作る**: DB・認証・UI の基盤を固めてから OCR 等の複雑な機能を追加
- **動作確認しながら積み上げる**: 各ステップで実際に動く状態を保つ
- **依存関係の少ない順に実装**: 後から機能を追加しやすい構造を優先

---

## 技術スタック（要件定義書 v4.0 準拠）

| カテゴリ | 技術 |
|---|---|
| フレームワーク | Next.js (App Router / TypeScript) |
| データベース | PostgreSQL（Neon） |
| ORM | Prisma |
| 認証 | NextAuth.js (Auth.js) + Google OAuth |
| OCR | Google Cloud Vision API |
| AI（食材分類・消費推定） | 未選定（Gemini が第一候補） |
| 対応ブラウザ | iOS: Safari / Android: Chrome / PC: Chrome |
| カメラ | `<input type="file" capture="environment">` |
| 状態管理 | Zustand + TanStack Query |
| UI | shadcn/ui + Tailwind CSS |
| ホスティング | Vercel |
| 画像ストレージ | Cloudflare R2 |

---

## ステップ一覧

| ステップ | 内容 | 対応機能 | フェーズ |
|---|---|---|---|
|

| Step 1 | DB スキーマ定義・マイグレーション | - | Phase 1 |
| Step 2 | ユーザー認証 + カスタム保管場所初期化 | F-06 | Phase 1 |
| Step 3 | 食材一覧画面（UI 基盤） | F-03 | Phase 1 |
| Step 4 | 食材 CRUD API + 手動入力・編集・削除 | F-04 | Phase 1 |
| Step 5 | レシート OCR + AI 保管場所分類 | F-01 | Phase 1 |
| Step 6 | 商品名正規化処理 | F-02 | Phase 1 |
| Step 7 | データ共有（複数端末対応） | F-10 | Phase 1 |
| Step 8 | PWA 対応 | - | Phase 1 |
| Step 9 | AIレシピベース消費 | F-14 | Phase 1 |
| Step 10 | 入力履歴サジェスト | F-08 | Phase 2 |
| Step 11 | 賞味期限管理 | F-05 | Phase 2 |
| Step 12 | 賞味期限自動推定 | F-15 | Phase 2 |
| Step 13 | 音声入力 | F-07 | Phase 3 |
| Step 14 | 買い物リスト自動生成 | F-16 | Phase 3 |
| Step 15 | 検索・絞り込み | F-13 | Phase 3 |
| Step 16 | オフライン対応 | F-09 | Phase 3 |
| Step 17 | プッシュ通知 | F-11 | Phase 3 |
| Step 18 | レシピ提案 | F-12 | Phase 3 |

---

## Step 0: 環境構築・プロジェクト初期設定

### 0-1. 開発ツールの確認・インストール

- [x] Node.js v20 LTS インストール確認
- [x] パッケージマネージャ: npm（標準）
- [x] VSCode 拡張機能インストール
  - `ESLint`
  - `Prettier - Code formatter`
  - `Tailwind CSS IntelliSense`
  - `Prisma`

### 0-2. Next.js プロジェクト作成

```bash
npx create-next-app@latest fridge-manager --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

### 0-3. 主要パッケージのインストール

```bash
# ORM
npm install prisma --save-dev
npm install @prisma/client

# 認証
npm install next-auth @auth/prisma-adapter

# 状態管理
npm install zustand @tanstack/react-query

# UI
npx shadcn@latest init
# 必要なコンポーネントを随時追加:
# npx shadcn@latest add button card tabs input dialog ...

# 画像アップロード（Cloudflare R2 / S3互換）
npm install @aws-sdk/client-s3

# Linter / Formatter
npm install --save-dev prettier eslint-config-prettier
```

### 0-4. 外部サービスのセットアップ

#### Neon（PostgreSQL）
1. neon.tech でアカウント作成
2. プロジェクト作成（リージョン: ap-northeast-1）
3. 接続文字列を取得 → `.env.local` に設定

#### Google Cloud
1. console.cloud.google.com でプロジェクト作成
2. Cloud Vision API を有効化
3. サービスアカウント作成 → JSON キーをダウンロード
4. OAuth 2.0 クライアント ID を作成（Google ログイン用）
   - 承認済みリダイレクト URI: `http://localhost:3000/api/auth/callback/google`

#### Cloudflare R2
1. Cloudflare ダッシュボードで R2 バケット作成
2. API トークン取得

#### Vercel
1. vercel.com でアカウント作成
2. GitHub リポジトリと連携（デプロイは後で）

### 0-5. 環境変数の設定

**`.env.local`**（ローカル開発用、`.gitignore` に含めること）

```env
# Database (Neon)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="openssl rand -base64 32 で生成"

# Google OAuth
GOOGLE_CLIENT_ID="xxxxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="xxxxx"

# Google Cloud Vision API
GOOGLE_CLOUD_VISION_CREDENTIALS='{"type":"service_account",...}'
# または
GOOGLE_APPLICATION_CREDENTIALS="./path/to/service-account.json"

# Cloudflare R2
R2_ACCOUNT_ID="xxxxx"
R2_ACCESS_KEY_ID="xxxxx"
R2_SECRET_ACCESS_KEY="xxxxx"
R2_BUCKET_NAME="fridge-receipts"
R2_PUBLIC_URL="https://xxxxx.r2.dev"
```

**`.env.example`**（テンプレートとして Git 管理）

```env
DATABASE_URL=
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CLOUD_VISION_CREDENTIALS=
AI_API_KEY=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
```

### 0-6. GitHub 連携

```bash
git add .
git commit -m "Initial commit: Next.js project setup"
git push origin main
git checkout -b develop
git push origin develop
```

### 0-7. フォルダ構成の確認

```
fridge-manager/
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   ├── auth/[...nextauth]/route.ts
│   │   │   ├── foods/route.ts
│   │   │   ├── foods/[id]/route.ts
│   │   │   └── ocr/route.ts
│   │   ├── (auth)/
│   │   │   └── login/page.tsx
│   │   ├── dashboard/page.tsx
│   │   ├── scan/page.tsx
│   │   ├── settings/page.tsx
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/
│   │   ├── ui/              # shadcn/ui コンポーネント
│   │   ├── food-card.tsx
│   │   ├── food-form.tsx
│   │   └── receipt-scanner.tsx
│   ├── lib/
│   │   ├── auth.ts          # NextAuth.js 設定
│   │   ├── prisma.ts        # Prisma クライアント（シングルトン）
│   │   ├── ocr.ts           # Cloud Vision API 連携   │   ├── ai-classifier.ts # AI 保管場所自動分類│   │   └── normalizer.ts    # 商品名正規化ロジック
│   ├── hooks/               # カスタムフック
│   ├── stores/              # Zustand ストア
│   └── types/               # TypeScript 型定義
├── public/
│   └── manifest.json        # PWA マニフェスト
├── .env.local
├── .env.example
├── package.json
├── tsconfig.json
├── next.config.js
├── tailwind.config.ts
└── README.md
```

**確認**: `npm run dev` でアプリが起動すること（Next.js デフォルトページが表示される）

---

## Step 1: DB スキーマ定義・マイグレーション
1
> 詳細設計: `docs/steps/step1-db-schema.md` / `docs/steps/step1-fridge-sharing.md`

### 1-1. Prisma 初期化

```bash
npx prisma init
```

### 1-2. schema.prisma の定義

要件定義書 v4.0 の「6. データベース設計」に記載の Prisma スキーマを使用。

**作成テーブル（Phase 1）**:
- `User`（googleId 直接保持、Account/Session/VerificationToken は不要）
- `Fridge`（冷蔵庫グループ）
- `FridgeMember`（ユーザーと冷蔵庫の紐づけ、1ユーザー1冷蔵庫）
- `FridgeInvitation`（招待リンク管理）
- `StorageLocation`（保管場所、Fridge に紐づく、ユーザーが自由に追加可能）
- `Food`（食材、fridgeId で冷蔵庫に紐づく、remainingStatus で残量管理）

**Phase 3 で追加予定**（Step 1 では作らない）:
- `NormalizationRule` / `ProductMaster`: 正規化はバックエンドコードで実装（Step 6）
- `Food.expiryDate`: Phase 3（F-05 賞味期限管理）で追加

### 1-3. マイグレーション実行

```bash
npm run db:migrate
# migration 名を入力: init
```

### 1-4. Prisma クライアントのシングルトン作成

**`src/lib/prisma.ts`**

```typescript
import { PrismaClient } from '@prisma/client'

// Next.js の HMR でインスタンスが増殖しないようにシングルトン化
const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }

export const prisma = globalForPrisma.prisma ?? new PrismaClient()

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

### 1-5. シードデータ

Step 1 ではシードデータなし（削除したテーブルに投入するデータがないため）。

**確認**: Prisma Studio で全テーブルが確認できること

```bash
npm run db:studio
```

---

## Step 2: ユーザー認証 + カスタム保管場所初期化

**対応機能**: F-06

### 2-1. NextAuth.js 設定

**`src/lib/auth.ts`**

- Google OAuth プロバイダー設定
- **JWT 戦略**（PrismaAdapter 不使用）
- `signIn` コールバックで `googleId` を使って User を upsert、初回ログイン時に Fridge を自動作成、デフォルト保管場所 3 つ（冷蔵庫/冷凍庫/常温）を自動作成
- `jwt` / `session` コールバックでセッションに `userId` / `fridgeId` を含める

**`src/app/api/auth/[...nextauth]/route.ts`**

- NextAuth ハンドラーのエクスポート

### 2-2. 認証プロバイダー

**`src/components/providers.tsx`**

- `SessionProvider` でアプリ全体をラップ
- TanStack Query の `QueryClientProvider` もここで設定

### 2-3. ログイン画面

**`src/app/(auth)/login/page.tsx`**

- Google Sign-In ボタン（shadcn/ui の Button コンポーネント）
- アプリロゴ・説明テキスト

### 2-4. 認証ガード（Middleware）

**`src/middleware.ts`**

- 未ログイン → `/login` にリダイレクト
- ログイン済み → ダッシュボードへ
- 保護対象パス: `/dashboard`, `/scan`, `/settings`, `/api/foods`, `/api/ocr`

### 2-5. ログアウト機能

- 設定画面にログアウトボタン配置

**確認**: Google アカウントでログイン・ログアウトできること。DB に User レコードが作成され、デフォルト保管場所（冷蔵庫/冷凍庫/常温）が自動作成されること

---

## Step 3: 食材一覧画面（UI 基盤）

**対応機能**: F-03

### 3-1. レイアウト設定

**`src/app/dashboard/layout.tsx`**

- BottomNavigation（モバイル）: 一覧 / スキャン / 設定
- レスポンシブ対応（PC ではサイドバー）

### 3-2. 食材一覧画面

**`src/app/dashboard/page.tsx`**

- 保管場所別タブ（デフォルト: 冷蔵庫 / 冷凍庫 / 常温 + ユーザー追加分）
- TanStack Query で API からデータ取得
- 登録日順でソート

### 3-3. 食材カードコンポーネント

**`src/components/food-card.tsx`**

- 食材名・残量ステータス・保管場所を表示
- 残量ステータスのアイコン表示
  - いっぱい: ●（満タン）
  - 半分: ◑（半分）
  - 少し: ○（少量）
- タップで詳細・編集へ遷移

### 3-4. 食材取得 API

**`src/app/api/foods/route.ts`** (GET)

- 認証済みユーザーの食材を取得
- クエリパラメータ: `storageLocationId`（保管場所フィルタ）
- 登録日順ソート

**確認**: ダミーデータ（Prisma Studio で手動投入）で一覧が正しく表示されること

---

## Step 4: 食材 CRUD API + 手動入力・編集・削除

**対応機能**: F-04

### 4-1. API ルート実装

**`src/app/api/foods/route.ts`** (POST)
- 食材の新規追加

**`src/app/api/foods/[id]/route.ts`** (GET / PUT / DELETE)
- 食材の取得・更新・削除

**`src/app/api/foods/[id]/consume/route.ts`** (PATCH)
- 残量ステータスの変更（いっぱい→半分→少し、または削除）

### 4-2. 食材追加・編集フォーム

**`src/components/food-form.tsx`**

- 食材名（テキスト入力）
- カテゴリ選択（セレクト）
- 残量ステータス（デフォルト「いっぱい」、半分/少しに変更可能）
- 保管場所選択（ユーザーの StorageLocation から選択）

### 4-3. 手動入力画面

**`src/app/dashboard/add/page.tsx`**

- 新規食材の手動入力フォーム
- レシートがない場合の入力手段

### 4-4. 食材操作 UI

- 食材カードからの簡易操作
  - 「消費」ボタン（残量ステータス変更: いっぱい→半分→少し、またはスワイプで消費済み削除）
  - 「編集」ボタン
  - 「削除」ボタン（確認ダイアログ付き）

### 4-5. Zustand ストア

**`src/stores/food-store.ts`**

- 選択中の保管場所タブ
- 一覧のフィルタ状態

**確認**: 食材の追加・編集・削除・消費が正しく動作すること

---

## Step 5: レシート OCR + AI 保管場所分類

**対応機能**: F-01

### 5-1. レシート撮影画面

**`src/app/scan/page.tsx`**

- `<input type="file" accept="image/*" capture="environment">` でカメラ起動
- ギャラリーからの選択も可能（`capture` 属性を外したボタン）
- 画像プレビュー表示

### 5-2. 画像アップロード API

**`src/app/api/ocr/route.ts`** (POST)

1. リクエストから画像を受け取る
2. Cloudflare R2 に画像を保存（参照用）
3. Google Cloud Vision API に画像を送信
4. OCR 結果をパース
   - テキストブロックから商品名を抽出（数量・単位は読み取らない）
   - レシート特有のフォーマットに対応（行ごとの解析）
5. 正規化エンジンで商品名を正規化
6. AI に正規化済み食材リストを送信し、保管場所を自動分類
7. 全食材の残量を「いっぱい」で初期設定
8. パース結果を JSON で返却

### 5-3. AI 保管場所自動分類

**`src/lib/ai-classifier.ts`**

- AI API（Gemini 等）に食材リストを送信
- ユーザーの StorageLocation リストもプロンプトに含める
- 各食材を適切な保管場所に分類して返却
- AI モデルは未選定（Gemini が第一候補、無料枠が大きいため）

### 5-3. Cloud Vision API 連携

**`src/lib/ocr.ts`**

- `@google-cloud/vision` パッケージを使用
- `TEXT_DETECTION` でテキスト認識
- レシートのパースロジック（商品名・金額の行分離）

```bash
npm install @google-cloud/vision
```

### 5-4. OCR 結果確認モーダル

**`src/components/scan-confirm-modal.tsx`**

- 認識された商品一覧をモーダルで表示
- 各アイテムの編集可能フィールド（食材名・保管場所・残量ステータス）
- 不要なアイテムの個別削除
- 「一括保存」ボタンで DB に保存

**確認**: レシートを撮影して商品名が認識され、AI が保管場所を自動分類し、確認モーダルで編集・保存できること

---

## Step 6: 商品名正規化処理

**対応機能**: F-02

### 6-1. 正規化エンジン

**`src/lib/normalizer.ts`**

OCR 読み取り直後にサーバーサイドで自動実行:

1. **カタカナ統一**: ひらがな → カタカナに変換
2. **修飾語除去**: コード内の修飾語リストにマッチする語を除去（DB 辞書は使わない）
3. **英語→日本語変換**: コード内の変換マップで変換（DB 辞書は使わない）
4. **カテゴリ推定**: 食材名のキーワードマッチングでカテゴリを推定（コードベース）
5. **LLMフォールバック**: 辞書でヒットしない表記について LLM で正規化（失敗時のみ呼び出し、コストを最小化）

### 6-2. 正規化 API

**`src/app/api/ocr/route.ts`** 内に組み込み

- OCR 結果 → 正規化 → カテゴリ判定 → AI 保管場所分類
- 一連のパイプラインとして処理

### 6-3. OCR フローへの組み込み

```
レシート画像 → Cloud Vision API → テキスト抽出
  → 正規化エンジン → カテゴリ判定 → AI 保管場所分類
  → 残量「いっぱい」で初期設定
  → 確認モーダルに表示
```

**確認**: 「おいしいポーク」→「豚」（カテゴリ: 肉）に変換されること

---

## Step 7: データ共有（冷蔵庫共有・招待機能）

**対応機能**: F-10

> 詳細設計: `docs/steps/step1-fridge-sharing.md`

### 7-1. 設計方針（DB 基盤は Step 1 で完了）

- 食材は `Fridge` に紐づく（`Food.fridgeId`）
- PostgreSQL を単一データソースとして使用（リアルタイム同期不要）
- 同一 Fridge のメンバーなら、どの端末・アカウントからでもデータを共有

### 7-2. 招待リンク機能（Step 7 の主実装）

- **`POST /api/fridge/invite`**: 招待リンク発行（UUID トークン、7 日間有効）
- **`GET /api/fridge/invite/[token]`**: 招待情報取得（確認画面用）
- **`POST /api/fridge/join/[token]`**: 冷蔵庫に参加（既存冷蔵庫から離脱→新冷蔵庫に参加）
- **`POST /api/fridge/leave`**: 冷蔵庫から離脱（新しい空の冷蔵庫を自動作成）
- **`GET /api/fridge/members`**: メンバー一覧取得

### 7-3. 招待 UI

- 設定画面にメンバー一覧・「招待リンクをコピー」ボタン・「離脱」ボタンを追加
- `/invite/[token]` ページ: 冷蔵庫名と「参加する」ボタンを表示

### 7-4. 競合解決

- 消費操作は `updatedAt` による楽観的ロック
  - `WHERE id = ? AND updatedAt = ?` で競合を検知
  - 競合時はエラーを返してクライアントに再取得を促す

**確認**: 招待リンクで別アカウントが同じ冷蔵庫に参加し、食材データが共有されること

---

## Step 8: PWA 対応

### 8-1. PWA マニフェスト

**`public/manifest.json`**

```json
{
  "name": "冷蔵庫マネージャー",
  "short_name": "myFridge",
  "start_url": "/dashboard",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#2563eb",
  "icons": [...]
}
```

### 8-2. next-pwa 設定

```bash
npm install next-pwa
```

- `next.config.js` に PWA 設定を追加
- Service Worker の自動生成

### 8-3. メタタグ設定

**`src/app/layout.tsx`**

- `<link rel="manifest">` の設定
- Apple 用メタタグ（`apple-mobile-web-app-capable` など）※ iOS Safari での PWA 対応に必須
- アイコン設定

**確認**: iOS Safari / Android Chrome で「ホーム画面に追加」ができ、スタンドアロンモードで起動すること

---

## Step 9: AIレシピベース消費

**対応機能**: F-14（Phase 1）

### 9-1. レシピ入力 UI

**`src/app/dashboard/consume/page.tsx`**

- レシピ名の入力フィールド（例: 「カレー」「肉じゃが」）
- 「食材を推定」ボタンで AI API を呼び出し

### 9-2. AI 消費推定 API

**`src/app/api/consume/suggest/route.ts`** (POST)

1. ユーザーの冷蔵庫の食材リストを DB から取得
2. レシピ名 + 食材リストを AI に送信
3. AI が使いそうな食材と残量変更を推定して返却

### 9-3. 消費確認モーダル

**`src/components/consume-confirm-modal.tsx`**

- AI が推定した食材一覧をモーダルで表示
- 各食材の残量変更内容を表示（半分→少し、少し→削除 等）
- ユーザーが食材の追加・削除・残量変更を編集可能
- 「確定」ボタンで一括更新

**確認**: レシピ名を入力して AI が食材を推定、確認モーダルで編集・確定後に残量が更新されること

---

## Step 10: 入力履歴サジェスト

**対応機能**: F-08（Phase 2）

### 10-1. 入力履歴の記録

- 食材追加時に `InputHistory` テーブル（または Food テーブルの集計）に記録
- 使用頻度をカウント

### 10-2. サジェスト UI

- 食材名入力時に候補をドロップダウン表示
- 使用頻度順にソート
- 部分一致検索

**確認**: 過去に入力した食材名が候補として表示されること

---

## Step 11: 賞味期限管理

**対応機能**: F-05（Phase 2）

### 11-1. DB スキーマ更新

- `Food` テーブルに `expiryDate: DateTime?` カラムを追加
- マイグレーションを実行

### 11-2. 賞味期限入力 UI

- 手動入力・編集画面に賞味期限（日付ピッカー）を追加
- 任意入力（null = 期限なし）

### 11-3. 期限切迫度カラー表示

- 食材カードに期限切迫度カラーを追加
  - 赤: 期限切れ・当日
  - 黄: 3 日以内
  - 緑: 4 日以上
- 賞味期限昇順ソートオプションを追加

**確認**: 賞味期限を入力した食材が期限切迫度に応じて色分けされること

---

## Step 12: 賞味期限自動推定

**対応機能**: F-15（Phase 2）

### 12-1. 賞味期限推定 API

**`src/app/api/foods/expiry-estimate/route.ts`** (POST)

1. 食材名を受け取る
2. LLM に食材名を送信し、一般的な賞味期限（日数）を推定
3. 冷蔵・冷凍・常温それぞれの保存期間を返却
4. 保管場所に応じた推定値を食材登録フォームの初期値としてセット

### 12-2. フォームへの組み込み

**`src/components/food-form.tsx`** 更新

- 食材名入力後に自動で推定 API を呼び出し
- 推定結果を賞味期限フィールドの初期値としてセット
- ユーザーは確認・変更可能（任意入力）

**確認**: 食材名を入力すると賞味期限の初期値が自動提案されること

---

## Step 13: 音声入力

**対応機能**: F-07（Phase 3）

### 13-1. 音声入力コンポーネント

**`src/components/voice-input.tsx`**

- マイクボタン（タップで録音開始/停止）
- Web Speech API（`webkitSpeechRecognition` / `SpeechRecognition`）を使用
- 日本語認識設定: `lang = 'ja-JP'`

### 13-2. 自然言語パース（LLM）

- Web Speech API の認識テキストを LLM に送信して解析
- 例: 「ほうれん草全部使い切った」→ `{ name: "ホウレンソウ", action: "delete" }`
- 例: 「卵あと2個くらい」→ `{ name: "卵", remainingStatus: "little" }`
- 例: 「昨日買った牛乳追加して」→ `{ name: "牛乳", action: "add" }`
- F-01 / F-14 と同じ AI モデルを使用

### 13-3. 確認フロー

- 認識結果を画面に表示
- 「確定」ボタン → 実行（残量ステータス変更・削除）
- 「やり直し」ボタン → 再録音

**確認**: 「〇〇使った」で食材の残量ステータスが変更されること

---

## Step 14: 買い物リスト自動生成

**対応機能**: F-16（Phase 3）

### 14-1. 買い物リスト生成画面

**`src/app/dashboard/shopping/page.tsx`**

- 冷蔵庫の残量「少し」食材を自動表示
- レシピ名を任意入力（F-14 と同様の入力フィールド）
- 「リスト生成」ボタンで AI API を呼び出し

### 14-2. 買い物リスト生成 API

**`src/app/api/shopping/suggest/route.ts`** (POST)

1. 冷蔵庫の残量が「少し」または「なし」の食材リストを DB から取得
2. 入力されたレシピ名（任意）と食材リストを LLM に送信
3. LLM が不足食材・補充が必要な食材をリスト化して返却

### 14-3. 確認・出力 UI

**`src/components/shopping-list-modal.tsx`**

- 生成されたリストをモーダルで表示
- 各アイテムの追加・削除・編集が可能
- 「コピー」ボタンでテキストとしてクリップボードにコピー
- 「シェア」ボタン（Web Share API）でシェア

**確認**: 買い物リストが生成され、コピー・シェアができること

---

## Step 15: 検索・絞り込み

**対応機能**: F-13（Phase 3）

### 15-1. 検索バー

- 食材名の部分一致検索（ひらがな/カタカナ/漢字対応）
- 検索履歴の保存

### 15-2. カテゴリフィルタチップ

- 複数カテゴリの同時選択
- カテゴリアイコン付きチップ UI（shadcn/ui Badge）

### 15-3. 複合検索

- カテゴリ + キーワード
- 保管場所 + カテゴリ

**確認**: 複合条件で正しく絞り込めること

---

## Step 16: オフライン対応

**対応機能**: F-09（Phase 3）

### 16-1. Service Worker + IndexedDB

- Workbox でキャッシュ戦略を設定
- IndexedDB（Dexie.js）でオフラインデータを保持

### 16-2. 同期キュー

- オフライン中の変更操作をキューに保存
- オンライン復帰時にバックグラウンド同期

**確認**: 機内モードで閲覧・入力が可能。復帰後にデータが同期されること

---

## Step 17: プッシュ通知

**対応機能**: F-11（Phase 3、F-05 実装後）

### 17-1. Web Push API + Service Worker

- 通知パーミッション取得
- サブスクリプション情報を DB に保存

### 17-2. 通知送信ロジック

- Vercel Cron Jobs または外部スケジューラで定期実行
- 賞味期限 3 日前に通知（Phase 2 の F-05・Step 12 実装後）
- 当日朝に通知

### 17-3. 通知設定画面

- 通知の ON/OFF
- 通知タイミングの設定

**確認**: 賞味期限が近い食材について通知が届くこと（iOS は Safari から PWA インストール後のみ、iOS 16.4+）

---

## Step 18: レシピ提案

**対応機能**: F-12（Phase 3）

### 18-1. レシピ検索ボタン

- 期限切れ間近の食材に「レシピ検索」ボタン表示（F-05 実装後）
- 任意の食材からもレシピ検索可能

### 18-2. 外部サービス連携

- Cookpad / Kurashiru への検索 URL を生成
- 新しいタブでブラウザを開く

**確認**: 食材名でレシピサイトの検索結果が開くこと

---

## テスト方針

| テスト種別 | 対象 | ツール | タイミング |
|---|---|---|---|
| ユニットテスト | 正規化ロジック、API ハンドラ | Vitest | 各 Step の実装後 |
| コンポーネントテスト | 主要な UI コンポーネント | Vitest + Testing Library | Step 3〜4 完了後 |
| E2E テスト | OCR→保存フロー、認証フロー | Playwright（Phase 1 完了後に導入検討） | Phase 1 完了後 |

---

## ブランチ運用

```
main          ← 本番環境（Vercel 自動デプロイ）
develop       ← 統合ブランチ（Vercel プレビュー）
feature/step-0-setup
feature/step-1-db-schema
feature/step-2-auth
feature/step-3-food-list
feature/step-4-food-crud
feature/step-5-ocr-ai
feature/step-6-normalizer
feature/step-7-sharing
feature/step-8-pwa
feature/step-9-recipe-consume
feature/step-10-suggest
feature/step-11-expiry
feature/step-12-expiry-estimate
feature/step-13-voice
feature/step-14-shopping-list
...
```

各 Step を feature ブランチで開発 → develop にマージ → 動作確認後 main にマージ

---

## 環境変数一覧

| 変数名 | 用途 | 取得元 |
|---|---|---|
| `DATABASE_URL` | PostgreSQL 接続文字列 | Neon |
| `NEXTAUTH_URL` | アプリの URL | localhost / Vercel |
| `NEXTAUTH_SECRET` | セッション暗号化キー | `openssl rand -base64 32` |
| `GOOGLE_CLIENT_ID` | Google OAuth クライアント ID | Google Cloud Console |
| `GOOGLE_CLIENT_SECRET` | Google OAuth シークレット | Google Cloud Console |
| `GOOGLE_CLOUD_VISION_CREDENTIALS` | Vision API 認証情報 | Google Cloud Console |
| `AI_API_KEY` | AI API キー（Gemini 等） | 未選定 |
| `R2_ACCOUNT_ID` | Cloudflare アカウント ID | Cloudflare Dashboard |
| `R2_ACCESS_KEY_ID` | R2 アクセスキー | Cloudflare Dashboard |
| `R2_SECRET_ACCESS_KEY` | R2 シークレットキー | Cloudflare Dashboard |
| `R2_BUCKET_NAME` | R2 バケット名 | Cloudflare Dashboard |

---

## 現在の状況

- [x] 要件定義書 v4.0 完成
- [x] 実装手順書 v3.0 完成
- [x] GitHub リポジトリ接続済み
- [x] **Step 0: 環境構築**
  - Node.js v20 確認
  - Next.js プロジェクト作成
  - 主要パッケージインストール済み
  - Docker 環境構築済み
  - `.env.example` 作成済み
  - GitHub 連携・Git hooks 設定済み
- [x] **Step 1: DB スキーマ定義・マイグレーション**
  - `prisma/schema.prisma` 作成（User/Fridge/FridgeMember/FridgeInvitation/StorageLocation/Food）
  - `prisma.config.ts` 作成（Prisma v7 対応）
  - `src/lib/prisma.ts` 作成（PrismaPg アダプター使用）
  - マイグレーション適用済み
  - Prisma Client 生成済み
- [x] **Step 2: ユーザー認証 + カスタム保管場所初期化**
  - `src/lib/auth.ts`（Google OAuth / JWT 戦略、初回ログイン時に Fridge + デフォルト保管場所を自動作成）
  - `src/app/api/auth/[...nextauth]/route.ts`
  - `src/app/(auth)/login/page.tsx`（Google Sign-In ボタン）
  - `src/components/Providers.tsx`（SessionProvider + QueryClientProvider）
  - `src/middleware.ts`（未ログインリダイレクト）
- [x] **Step 3: 食材一覧画面（UI 基盤）**
  - `src/app/dashboard/page.tsx`（保管場所別タブ、TanStack Query でデータ取得）
  - `src/app/dashboard/layout.tsx`（BottomNavigation）
  - `src/components/FoodCard.tsx`（残量ステータスアイコン表示）
  - `src/app/api/foods/route.ts` (GET)
- [x] **Step 4: 食材 CRUD API + 手動入力・編集・削除**
  - `src/app/api/foods/route.ts` (POST)
  - `src/app/api/foods/[id]/route.ts` (GET / PUT / DELETE)
  - `src/app/api/foods/[id]/consume/route.ts` (PATCH)
  - `src/components/FoodForm.tsx`
  - `src/stores/foodStore.ts`
- [x] **Step 5: レシート OCR + AI 保管場所分類**
  - `src/app/scan/page.tsx`（OS標準カメラ起動 / ボタン1つでスキャン開始 / プレビュー表示 / 自動スキャン）
  - `src/app/api/ocr/route.ts`
  - `src/lib/vision.ts`（Google Cloud Vision API 連携）
  - `src/lib/ai.ts`（AI 保管場所自動分類）
  - `src/components/ScanConfirmModal.tsx`（確認モーダル）
- [x] **Step 6: 商品名正規化処理**
  - `src/lib/normalizer.ts`（カタカナ統一 / 修飾語除去 / 英語→日本語変換 / LLM フォールバック）
  - `src/lib/ocr.ts`（レシートテキスト解析 + 正規化パイプライン）
- [x] **Step 7: データ共有（冷蔵庫共有・招待機能）**
  - `src/app/api/fridge/invite/route.ts` / `invite/[token]/route.ts`
  - `src/app/api/fridge/join/[token]/route.ts`
  - `src/app/api/fridge/leave/route.ts`
  - `src/app/api/fridge/members/route.ts`
  - `src/app/invite/[token]/page.tsx`
  - `src/components/SettingsClient.tsx`（メンバー一覧・招待リンク・離脱）
- [ ] **Step 8: PWA 対応**（次のアクション）
- [x] **Step 9: AIレシピベース消費**
  - `src/app/api/consume/suggest/route.ts`
  - `src/app/dashboard/consume/page.tsx`
  - `src/components/ConsumeConfirmModal.tsx`

---

## 変更履歴

### v3.3（Phase 再編: 音声入力・買い物リストを Phase 3 に移動）

- **Step 10**: 入力履歴サジェスト（旧 Step 11）に変更
- **Step 11**: 賞味期限管理（旧 Step 12）に変更
- **Step 12**: 賞味期限自動推定（旧 Step 13）に変更
- **Step 13**: 音声入力 F-07 を Phase 3 に移動（旧 Step 10 Phase 2）
- **Step 14**: 買い物リスト自動生成 F-16 を Phase 3 に変更

### v3.2（LLM活用拡大・賞味期限管理昇格）

- **Step 6 更新**: 正規化エンジンに LLM フォールバックを追加（辞書ヒット失敗時のみ）
- **Step 10 更新**: 音声入力の自然言語解析を正規表現 → LLM に変更
- **Step 12 フェーズ変更**: 賞味期限管理（F-05）を Phase 3 → Phase 2 に昇格
- **Step 13 新規追加**: 賞味期限自動推定（F-15、Phase 2）
- **Step 14 新規追加**: 買い物リスト自動生成（F-16、Phase 2）
- **Step 15〜18**: 旧 Step 13〜16 をリナンバー

### v3.1（F-14 をコア機能に昇格）

- **Step 9 フェーズ変更**: AIレシピベース消費（F-14）を Phase 2 → Phase 1 に昇格
- AI 統合が既に OCR 後の保管場所分類（Step 5）で必要なため、同一フェーズに集約
- Phase 2 は音声入力（Step 10）・履歴サジェスト（Step 11）のみに変更

### v3.0（賞味期限・残量・保管場所・AI 統合の見直し）

- **要件定義書 v4.0 に準拠**
- **賞味期限**: Phase 1 では実装しない。Step 12（Phase 3）で `Food.expiryDate` カラム追加 + カラー表示を実装
- **残量モデル変更**: `quantity: Float` + `unit: String` → `remainingStatus: 'full' | 'half' | 'little'`
- **保管場所カスタム化**: 固定文字列 → `StorageLocation` テーブル。デフォルト 3 つ（冷蔵庫/冷凍庫/常温）+ ユーザー自由追加
- **AI 統合**: Step 5 に AI 保管場所自動分類を追加。Step 9 に AI レシピベース消費を新規追加
- **Step 再編**: Step 9=AIレシピベース消費, Step 10=音声入力, Step 11=サジェスト, Step 12=賞味期限, Step 13-16=その他 Phase 3
- **環境変数**: `AI_API_KEY` を追加
- **確認モーダル**: OCR 後の確認画面をモーダル形式に変更

### v2.1（Step 1 完了）

- **Prisma v7 対応**: `prisma.config.ts` 導入、`schema.prisma` から `url` を削除
- **PrismaClient**: `@prisma/adapter-pg` を使用したドライバーアダプター方式に変更
- **DB スキーマ**: 冷蔵庫共有モデルを導入（Fridge/FridgeMember/FridgeInvitation）
- **Step 6 更新**: 正規化ロジックを DB 辞書からバックエンドコードに変更
- **Step 7 更新**: Firestore 同期 → 冷蔵庫グループ+招待リンク方式に変更

### v2.0（v1.0 からの変更）

- **技術スタック全面変更**: Flutter / Firebase / SQLite → Next.js / PostgreSQL / Prisma
- **Step 構成の再編**: 認証を Step 2 に前倒し（API 保護のため早期に必要）
- **Step 7 変更**: Firestore 同期 → PostgreSQL 単一構成（Web アプリのため同期不要）
- **Step 8 追加**: PWA 対応（ネイティブアプリ相当の体験に必要）
- **テストツール変更**: Flutter Test → Vitest / Testing Library / Playwright
- **環境変数一覧を追加**: 必要なキー・取得元を明記
